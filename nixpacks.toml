providers = ["node"]

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["npm run build", "npx prisma generate"]

[start]
cmd = "npx tsx server/index.ts"
